services:
  - type: web
    name: job-hunter
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: ACCESS_CODE
        generateValue: true
        sync: false
      - key: PYTHONUNBUFFERED
        value: "1"
    healthCheckPath: /api/health
    autoDeploy: true